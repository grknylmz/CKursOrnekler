#include "student.h"



int cmpStudent1(const Student *p1,const Student *p2){
	int cmpResult = strcmp(p1->fname,p2->fname);
	if(cmpResult)
		return cmpResult;
	cmpResult = strcmp(p1->fname,p2->fname);
		
		
}